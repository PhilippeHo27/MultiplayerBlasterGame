[attr]lock filter=lfs diff=lfs merge=binary -text lockable
[attr]lockonly lockable
[attr]lfs filter=lfs diff=lfs merge=binary -text
[attr]lfstext filter=lfs diff=lfstext merge=lfstext -text

# Unreal Engine file types
*.umap lock
*.locres lfs
*.locmeta lfs

# Steam Audio files
*.phononscene lfs
*.probebox lfs
*.probebatch lfs
*.bakedsources lfs

# Binaries
*.exe lfs
*.dll lfs
*.rcc lfs

# FMOD files
*.bank lfs
*.wav lfs
*.mp3 lfs
*.ogg lfs
*.flac lfs

# Images
*.png lfs
*.ico lfs
*.icns lfs

# Movies
*.bk2 lfs

# Additional file types (if needed)
# Add here any additional file extensions specific to your project
